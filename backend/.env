# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/procurement_db

# JWT Secret Key
JWT_SECRET=procurement_secret_key_2024_change_this_in_production

# Optional: MongoDB with authentication (if needed)
# MONGODB_URI=mongodb://username:password@localhost:27017/procurement_db?authSource=admin

# Optional: External MongoDB (MongoDB Atlas)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/procurement_db?retryWrites=true&w=majority
